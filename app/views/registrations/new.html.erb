<% content_for :title, t(".title") %>
<div class="mx-auto max-w-md">
  <h1 class="mb-6 text-xl font-bold text-text"><%= t(".heading") %></h1>
  <%= render "registrations/form" %>
  <p class="mt-5 text-sm text-text-dim">
    <%= t(".already_have_account") %>
    <%= link_to t(".sign_in"), new_session_path, class: "text-brass underline hover:text-brass-hi" %>
  </p>
</div>
